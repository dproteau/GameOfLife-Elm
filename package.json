{
  "name": "gameoflife",
  "version": "1.0.0",
  "description": "Conway's Game of Life in Elm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:elm": "rimraf elm-stuff/build-artifacts",
    "format:elm": "elm format src/ --yes",
    "watch:elm": "chokidar-cmd -c \"elm make src/Main.elm --output app/scripts/elm.js & echo -- -=-=-=-=-=-=-=-=-=- -- \" -t src/**/*.elm  --initial"
  },
  "author": "David Proteau",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cmd": "^1.2.1",
    "elm": "^0.18.0",
    "elm-format": "^0.6.0-alpha.6",
    "rimraf": "^2.6.1"
  }
}
